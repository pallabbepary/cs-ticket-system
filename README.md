What is JSX, and why is it used?
Ans: JavaScript XML, JSX is a syntax extension for JavaScript. It allows you to write structure that looks remarkably like HTML directly within your script files.
What is the difference between State and Props?
Ans: In the world of React, Props and State are the two types of data that control a component, but they play very different roles.
What is the useState hook, and how does it work?
Ans:The useState hook works by preserving a value between component re-renders and providing a mechanism to update that value and trigger a new render. 
How can you share state between components in React?
Ans:
How is event handling done in React?
Ans: Defining functions (event handlers) within components and passing them as props to elements using camelCase syntax.
